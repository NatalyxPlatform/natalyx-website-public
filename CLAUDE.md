# Claude — context bootstrap (thin pointer, no imports)

Pull context on demand: the `/natalyx-context` skill (cross-repo map, product
direction, boundaries), then `./AGENTS.md` (this repo's map). Workspace rules:
root `C:\Natalyx\AGENTS.md`. Synthetic data only; ask before pushes.
